<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 
      ВИПРАВЛЕННЯ 1: Встановлено РЕАЛЬНУ, стабільну версію Spring Boot
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <!-- 
          ВИПРАВЛЕННЯ:
          Оновлено до версії 3.5.6, яку ви вказали. Дякую за уточнення!
        -->
        <version>3.5.6</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>ua.edu.ucu.apps</groupId>
    <artifactId>Lab5.1</artifactId>
    <version>1.0-SNAPSHOT</version>

    <!-- 
      ВИПРАВЛЕННЯ 2: Вказано версію Java 17, яка потрібна для Spring Boot 3+
    -->
    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.3</version> <!-- Тут версія потрібна -->
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
            <!-- ВИПРАВЛЕННЯ 3: Видалено зайву <version> -->
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <!-- ВИПРАВЛЕННЯ 4: Видалено зайву <version> -->
            <optional>true</optional>
        </dependency>
    </dependencies>

    <!-- 
      ВИПРАВЛЕННЯ 5: Додано плагін, необхідний для збірки Spring Boot проєкту
    -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>


